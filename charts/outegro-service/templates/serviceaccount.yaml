{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "outegro.serviceAccountName" . }}
  labels: {{- include "outegro.labels" . | nindent 4 }}
{{- end }}
